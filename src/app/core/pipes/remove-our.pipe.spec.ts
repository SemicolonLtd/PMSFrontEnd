import { RemoveOurPipe } from './remove-our.pipe';

describe('RemoveOurPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveOurPipe();
    expect(pipe).toBeTruthy();
  });
});
